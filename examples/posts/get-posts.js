/**
 * This function shows how to retrieve n posts and specify options for how they should be retrieved. 
 * The options are:
 * - sortBy - sortBy can be createTime, threadUpdateTime (which will return root posts sorted by the the postTime of the most recent post in the thread)
 * TODO: Make these option names similar to what the terms are in patchwork. 
 */